---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" description="About the survivor - profile and mission data.">
  <article class="about">
    <div class="page-header">
      <div class="status-bar">
        <span class="status-indicator">
          <span class="dot"></span>
          <span>SURVIVOR PROFILE</span>
        </span>
        <span class="classification">CLASSIFIED</span>
      </div>
      <h1 class="page-title glitch" data-text="Survivor">Survivor</h1>
      <p class="page-subtitle">Operator // Digital Frontiersman</p>
    </div>
    
    <section class="content">
      <div class="data-block">
        <div class="block-header">
          <span class="block-icon">◈</span>
          <h2>Transmission Origin</h2>
        </div>
        <p>
          Welcome to the digital frontier. This blog is a secure channel for exploring technology,
          programming, and the ever-evolving landscape of the digital wasteland. All transmissions
          are encrypted and logged for posterity.
        </p>
      </div>

      <div class="data-block">
        <div class="block-header">
          <span class="block-icon">◈</span>
          <h2>Mission Directive</h2>
        </div>
        <p>
          To document the journey through code, share intelligence, and contribute to the
          open-source resistance. Every post is a piece of the puzzle in understanding
          our technological future. Survival through knowledge.
        </p>
      </div>

      <div class="data-block">
        <div class="block-header">
          <span class="block-icon">◈</span>
          <h2>Areas of Operation</h2>
        </div>
        <ul class="skill-list">
          <li><span class="bullet">▸</span>Software Development</li>
          <li><span class="bullet">▸</span>Web Technologies</li>
          <li><span class="bullet">▸</span>System Administration</li>
          <li><span class="bullet">▸</span>Open Source Projects</li>
          <li><span class="bullet">▸</span>Tech Philosophy</li>
        </ul>
      </div>

      <div class="data-block">
        <div class="block-header">
          <span class="block-icon">◈</span>
          <h2>Communication Channels</h2>
        </div>
        <p>
          Establish contact through secure frequencies. This transmission hub is hosted at
          <span class="accent">blog.terrerov.cu</span>. Signal strength optimal.
        </p>
      </div>
    </section>

    <div class="footer-sig">
      <div class="sig-line"></div>
      <span class="sig-text">END TRANSMISSION</span>
      <div class="sig-line"></div>
    </div>
  </article>
</BaseLayout>

<style>
  .about {
    max-width: 720px;
  }

  .page-header {
    margin-bottom: 2.5rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(196, 77, 45, 0.2);
  }

  .status-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.25rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .status-indicator {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.4rem 0.8rem;
    background: rgba(0, 255, 100, 0.05);
    border: 1px solid rgba(0, 255, 100, 0.2);
    border-radius: 2px;
    font-family: var(--font-heading);
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: #00ff64;
    text-transform: uppercase;
  }

  .status-indicator .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #00ff64;
    box-shadow: 0 0 8px #00ff64;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  .classification {
    font-family: var(--font-heading);
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.3em;
    color: var(--color-accent);
    padding: 0.3rem 0.6rem;
    border: 1px solid rgba(196, 77, 45, 0.3);
    text-transform: uppercase;
  }

  .page-title {
    font-size: clamp(2rem, 6vw, 3rem);
    margin: 0 0 0.75rem 0;
    padding: 0;
    border: none;
  }

  .page-subtitle {
    color: var(--color-muted);
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    margin: 0;
    font-family: var(--font-heading);
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .data-block {
    background: rgba(12, 12, 15, 0.5);
    border: 1px solid rgba(196, 77, 45, 0.1);
    border-radius: 4px;
    padding: 1.5rem;
    position: relative;
    transition: all 0.3s ease;
  }

  .data-block::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 100%;
    background: var(--color-accent);
    opacity: 0.3;
    border-radius: 4px 0 0 4px;
    transition: opacity 0.3s ease;
  }

  .data-block:hover {
    background: rgba(15, 15, 20, 0.6);
    border-color: rgba(196, 77, 45, 0.25);
  }

  .data-block:hover::before {
    opacity: 1;
  }

  .block-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .block-icon {
    color: var(--color-accent);
    font-size: 0.7rem;
    opacity: 0.7;
  }

  .block-header h2 {
    font-size: 0.8rem;
    margin: 0;
    color: var(--color-text-bright);
    letter-spacing: 0.15em;
  }

  .content p {
    color: var(--color-muted);
    margin: 0;
    line-height: 1.75;
    font-size: 0.95rem;
  }

  .skill-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.5rem;
  }

  .skill-list li {
    color: var(--color-muted);
    padding: 0.5rem 0;
    padding-left: 1.25rem;
    position: relative;
    font-size: 0.95rem;
    transition: all 0.2s ease;
  }

  .skill-list .bullet {
    position: absolute;
    left: 0;
    color: var(--color-accent);
    transition: transform 0.2s ease;
  }

  .skill-list li:hover {
    color: var(--color-text-bright);
    padding-left: 1.5rem;
  }

  .skill-list li:hover .bullet {
    transform: translateX(3px);
  }

  .accent {
    color: var(--color-accent);
    font-weight: 500;
  }

  .footer-sig {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-top: 3rem;
    padding-top: 2rem;
  }

  .sig-line {
    flex: 1;
    max-width: 80px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-accent), transparent);
    opacity: 0.4;
  }

  .sig-text {
    font-family: var(--font-heading);
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.3em;
    color: var(--color-accent);
    text-transform: uppercase;
    opacity: 0.6;
  }

  @media (max-width: 768px) {
    .page-header { padding-bottom: 1.5rem; margin-bottom: 2rem; }
    .data-block { padding: 1.25rem; }
    .status-bar { flex-direction: column; align-items: flex-start; }
  }

  @media (max-width: 480px) {
    .data-block { padding: 1rem; }
    .block-header h2 { font-size: 0.75rem; }
    .content p { font-size: 0.9rem; }
  }
</style>
